# Worksをスプレッドシートから追加する方法

既存のGoogleスプレッドシートの「works」シートを使います。

1. 1行目の既存列の右側に、次の列名を追加します。
   `表示, 日付, 作品名, 分類, リンク, 画像URL, コラボ名, 掲載先・収録先, 補足, 説明`
2. 作品を追加する行では、先頭4列を次のように入力します。
   `page=works`, `selector=.works-sheet-item`, `mode=work_item`, `value=1`
3. 「表示」を `TRUE` にするとサイトへ表示されます。`FALSE` にすると非表示です。

## 入力項目

- 日付: `2026/09/08` 形式。Works内では日付順に並びます。
- 作品名: 必須です。
- 分類: `music_game` または `音楽ゲーム`、`commission` または `楽曲提供`。
- リンク: YouTube、Spotify、その他のページURLに対応します。
- 画像URL: YouTubeの場合は空欄で構いません。サムネイルを自動取得します。
- コラボ名、掲載先・収録先、補足、説明: 必要な項目だけ入力します。

同じ「作品名」と「日付」のカードが既にある場合は重複追加しません。
